# PROGRESS.md

**Resume here:** **Phase D is complete.** The author asked (2026-09-10) for Modules 3, 4, 5, 6, 9, 10, 12, 14, 15 and 18 in one session (an explicit exception to the usual one-module-per-session rule); all ten are built, verified, and committed. Combined with the parallel session's own list (8, 11, 13, 16, 17, 19) and the modules built in earlier sessions (0, 1, 2, 7), **every module from 0 through 19 is now built, verified, and linked** (`course.js`: all 20 entries `built: true`; the nav pager runs end to end from the introduction through the capstone with no gaps). This is a bigger milestone than the author's specific request: the request is done, and so is the phase it was part of. **Do not start Phase E (the verification pass) or Phase F (ship) without the author asking** — tell the author Phase D is complete and wait for direction, per this project's standing "do not ask permission to continue routine building, but do not start a new phase unprompted" balance. If a future session is told to continue with no more specific instruction than "keep going," the reasonable next step is Phase E as CLAUDE.md defines it (reread every module against: every number traceable to `results/`; every example labelled real-with-source or illustrative; every citation resolves; sigma estimate stated for every index; stability shown before every capability; p-values explained correctly everywhere; metric units only; no employer content; no placeholders; every quiz scores correctly; every calculator reproduces its page's example) — but confirm with the author first rather than assuming.

**Published (2026-09-10):** at the author's request the course was pushed to GitHub as `iamerdemyilmaz/sixsigma` and is served by GitHub Pages at https://erdemyilmaz.me/sixsigma/ (project site under the portfolio's custom domain), listed as a card on the portfolio home page. Cloudflare Web Analytics beacon (cookieless, aggregate counts) added to all 22 pages. Phase E (the full verification reread) has **not** been run; the course is live on the strength of the per-module `verify_all.sh` passes. Running Phase E is the recommended next step and any corrections it produces are deployed by pushing to `main`.

**Method used throughout Phase D** (kept here for Phase E or any future module work, e.g. if the curriculum changes): read `SOURCES.md`'s "### Module N" section for the outline/sources/planned examples, register datasets in `generate.py` with prefix `m0N-`/`m1N-` (confirm the exact file slug against `course.js`'s `MODULES` array before naming any new page — Module 14's slug turned out to be `14-lean-tools`, not the longer name first assumed, and this recurred more than once), add any new `kind` to `compute.py` + `recompute.py` + `sanity.py` together (only add a `stats.js`/`calculators.js` engine if one of the nine required calculators actually fits, or if an existing kind already has one and simply gets reused, as Module 14's `ttest2`/`p` and Module 18's `capability` did), write the page following the reference module's structure (numbers computed by script, quoted via `data-ex`/`data-key`/`data-dp` spans, hand intermediates go in `allowed-numbers` after being computed by script — and always read a table's values from the actual generated CSV, never from memory of a script's printed summary statistics; Module 15's exercise 2 hand-fabricated a data table from memory and `sanity.py` caught it), set `built: true` in `course.js`, run `bash verification/verify_all.sh` in the background (by the end of Phase D this took several minutes, growing slower as `tolerance_stack`'s Monte Carlo simulations added real runtime) and do other useful work while waiting rather than polling repeatedly, spot-check the page in a browser (`.claude/launch.json` `"static"` config serves the repo on port **8781**, renegotiated between sessions — check the file's current content before assuming a port; the Browser pane may report itself "hidden," in which case use `read_page`/`get_page_text`/`javascript_tool` DOM checks instead of screenshots, and grade the quiz via `window.SIXSIGMA.gradeQuiz(form)` or by filling in radios and clicking submit), update `EXAMPLES.md` and this file, check `git status --short` for concurrent-session changes before every `git add`, commit.

**Parallel session (2026-09-09/10):** a second session, on Fable 5.1, was asked by the author to build Modules 8, 11, 13, 16, 17 and 19 in the same working tree while this list (3, 4, 5, 6, 9, 10, 12, 14, 15, 18) proceeds. That session's list is complete (commits through "Phase D: Module 19 (Capstone)"); it is not expected to make further changes, but the working tree is still shared and another parallel session could resume. Both sessions edit `verification/generate.py`, `compute.py`, `recompute.py`, `sanity.py`, `test_calculators.js`, `assets/js/course.js`, `EXAMPLES.md`, `SOURCES.md` and this file: use targeted edits or line inserts, never whole-file rewrites; re-read a shared file before editing it (check `git status`/`git diff` for foreign changes before every `git add`); commit only your own module's files plus the shared files after checking that `git diff --cached` contains only your own hunks — if a shared file's changes are interleaved with another session's in the working tree, don't just `git add` the whole file: either use `git apply --cached` on a hand-built patch, or (cleanest) construct the exact blob you want staged with `git hash-object -w` from `git show HEAD:path` plus only your own inserted text, then `git update-index --cacheinfo 100644,<blob>,path` — this stages your change without touching the working file at all, so the other session's uncommitted work in the same file is untouched. **Note (2026-09-10):** this discipline slipped once — the Module 19 commit's `generate.py` diff (+185 lines) turned out to include this session's already-written Module 4 dataset registrations (`m04-grr-scale`, `m04-bias`, `m04-linearity`, `m04-stability`, `m04-attribute-braze`) along with the m19 ones, apparently from a plain `git add`/whole-file stage rather than a split. The content was correct either way (verified before and after), so nothing was lost, but Module 4's own commit ended up smaller than expected because that part of it was already in `HEAD`. Not worth fixing after the fact (no rebasing of another session's already-pushed-adjacent commit); just keep splitting carefully going forward so it does not compound.

**New harness kinds needed, by module (worked out 2026-09-10, before building any of Modules 4–18 in this batch):**
- **Module 4 (MSA):** new kind `attribute_agreement` (columns part, appraiser, trial, rating[, standard]; Fleiss' kappa across appraisers, Cohen's kappa per appraiser vs a standard if given, within-appraiser repeatability, % agreement) for `ex04-attribute-visual`. Bias = one-sample t-test of (measured − reference) against 0, reuse `ttest1`. Linearity = regression of bias against reference value across levels, reuse `regression`. Stability = I-MR of repeated readings on one reference part over time, reuse `imr`. Crossed R&R reuses `grr` (`m04-grr-bore` already registered in Phase C; add `m04-grr-scale` for the second, tighter example the outline calls for).
- **Module 5 (sampling):** no new kind. Rational subgrouping demo = the same 100 individuals subgrouped two ways (`capability`/`xbar_r` twice, same values, different subgroup assignment). Rounding-loss demo = the same values rounded to two resolutions (`descriptive` twice).
- **Module 6 (DPMO/RTY):** one new kind, `rty_chain` (params: `yields` list → RTY = product, normalized yield = RTY^(1/k), total DPU = −ln(RTY), cumulative RTY by step). `dpmo` and `sigma_table` already exist and cover the rest; note `Calc.dpmo` in `calculators.js` already has an RTY section (`S.rty`) wired up client-side with default yields `[0.98, 0.95, 0.99, 0.97, 0.985]` — reuse that exact list as `m06-rty-chain` so the calculator's default matches the page.
- **Module 9 (graphical):** one new kind needed, `multivari` (columns time, part, x, K position-replicates per (time,part) row group → positional σ from Rbar/d2 on within-part ranges, cyclical σ from Rbar/d2 on part-mean ranges within each time, temporal σ from the plain sample sd of the time-period means — a simplified range-based decomposition consistent with the course's existing Rbar/d2 style, not a formal ANOVA F-test, because Module 9 is graphical/exploratory and precedes hypothesis testing in Module 11). `pareto` does **not** need building — the parallel session already added it for Module 19 (`compute.py`/`recompute.py`/`sanity.py`, columns `category`, `count` → sorted descending, cumulative %; `check_pareto` in `sanity.py`); reuse it directly. Box-plot-by-shift reuses `descriptive` once per shift. Anscombe reuses the existing `m01-anscombe-*` examples (just redeclare them).
- **Module 10 (root cause):** two small new kinds, both pure arithmetic. `rpn` (rows of failure mode + S/O/D 1–10 ratings → RPN = S×O×D, sorted; Action Priority H/M/L is explained in prose only, as a simplified illustrative rule citing S-A8/S-E17, since the official AIAG-VDA AP lookup table is not available at more than secondary depth — do not reproduce a fabricated "official" AP table). `fault_tree` (a small AND/OR gate tree with leaf probabilities → top-event probability, P(AND)=∏, P(OR)=1−∏(1−p)).
- **Module 12 (regression):** one significant new kind, `mregression` (multiple linear regression, 3 predictors). `compute.py` via `statsmodels` (`smf.ols("y ~ x1 + x2 + x3")`). `recompute.py` needs a genuinely different route: solve the normal equations XᵀXβ=Xᵀy by hand (Gauss-Jordan elimination, no numpy) and invert XᵀX the same way to get coefficient SEs; `recompute.py` already has `t_cdf`/`f_sf` from the simple-regression work, reuse them. Simple regression and residual analysis reuse the existing `regression` kind and its `fitted`/`residuals` fields; Anscombe reuses `m01-anscombe-*`.
- **Module 14 (Lean tools):** one tiny new kind, `pugh_matrix` (concepts × criteria scores relative to a baseline → row totals, pure summation). SMED before/after reuses `ttest2`. The leak-rate pilot before/after reuses the `p` chart kind (no new proportions-test kind added; the p-chart's own shift/rule evidence carries the "before/after with a control chart" point).
- **Module 15 (variation/design):** two new kinds. `tolerance_stack` (parts with nominal/tolerance/assumed Cpk[/mean shift in σ] → worst-case = Σtol, RSS = √Σtol², closed-form predicted stack Cpk from RSS assuming centring, PLUS a seeded Monte Carlo of the actual stack distribution to show how a real mean shift degrades capability even though RSS correctly predicted the spread — ties back to the Cp-vs-Cpk/centring lesson from Module 7; `compute.py`'s Monte Carlo uses `numpy`, `recompute.py`'s must be an independently-seeded Monte Carlo using stdlib `random` (not the same draws — compare statistically with a generous tolerance, the same pattern already used for `funnel_growth` in Module 2, not exact equality). `taguchi_loss` (raw x data like `descriptive`, plus params `target`, `A0` (cost at the spec limit), `delta0` (half-tolerance) → k = A0/delta0², average loss = k·(s² + (mean−target)²)). Tolerance allocation reuses `tolerance_stack`'s RSS building block on a second, allocation-flavoured registration; no new kind.
- **Module 18 (control plans):** no new kind. "Ongoing Cpk trend" = several months of data as separate `capability` registrations, read off the existing `overall.Ppk_ci95` field already computed by that kind — the point (don't react to CI-overlapping wobble) needs no new machinery. Control plan and reaction plan tables are qualitative.

Do not re-derive this plan from scratch in a fresh session; re-read this section instead. If a module's actual construction reveals the plan above needs to change, update this section rather than silently diverging from it.

**Phase C status (for reference):** `index.html`, `calculators.html`, `assets/css/course.css`, `assets/js/course.js`, `assets/js/calculators.js` (all nine calculators) and the reference module `modules/07-capability-1.html`; `bash verification/verify_all.sh` passes (34 examples, 100 page checks).

**Recommended model:** a mid-tier model (Sonnet) for Phase D Modules 2 to 6, because the structure is fixed by Modules 7, 0 and 1 and `verify_all.sh` catches numerical errors; the strongest model again for Modules 8, 11, 13, 16, 17 and 19. (The author chose to continue with Fable 5.1 for Module 1; Sonnet built Module 2.)

**Open decisions waiting on the author:**

1. Browser review of Phase C and of Modules 0, 1 and 2 (the author said "start working on phase D", which was taken as approval of Phase C; a review of the new pages is still welcome, in particular Module 0 Section 6 on the critiques and Module 1's length). Points to look at: the two-convention capability table (Table A) and the calculator's convention toggle; the amount of calculator output repeated for Examples 2 and 3 and the exercises (inputs collapsed, output shown); the "Why this matters" illustrative failure in place of a real case (Module 7 has no verified public case with data; Kane 1986 and Wheeler 2019 carry the verified history and argument). For Module 2 specifically: the red bead experiment's box/paddle numbers are the course's own constructed choice, not Deming's documented figures (no source found states them; see SOURCES.md S-E4's note and decision 8 below) — worth a second look if the author knows a better-sourced number; and whether the funnel experiment's Monte Carlo growth table (150 × 40-drop replications, `m02-funnel-growth`) is pitched at the right level of statistical detail for this audience or should be trimmed.
2. Whether `calculators.html` may keep the Module 4 and Module 11 preload datasets (`m04-grr-bore`, `m11-ttest2-pull`, `m11-anova-machines`, registered and verified now so that the gauge R&R and test calculators are not empty) before those modules are written. The factorial calculator is preloaded with the published Montgomery plasma-etch example instead.
3. The proposed deployment slug `six-sigma/` on erdemyilmaz.me (nothing depends on it).

**Decisions made (2026-09-09):**

1. Module outlines and example plans in `SOURCES.md` Part 2: **approved**.
2. Capstone: **brazed aluminium heat-exchanger line**, leak-test reject rate driven by braze-joint gap. The bore Ø12.000 ± 0.025 mm stays the running example for Modules 7, 8, and 16.
3. AIAG MSA-4 and PPAP-4 thresholds: cite as guidelines, marked "confirmed via secondary sources". The AIAG & VDA SPC Manual's own target tables (S-A2, Tables 8-1 and 9-3) are cited directly with page numbers.
4. Sony television story: told "as reported by Taguchi and Clausing and by Phadke", labelled not independently verified.
5. License: **CC BY-NC-SA 4.0 for content, MIT for code**.
6. Deployment: the course is published on the author's portfolio site **erdemyilmaz.me** as a new course alongside the DFx course at `https://erdemyilmaz.me/dfx/`. Phase C must match the DFx course's look, navigation, footer, and URL layout (see "Deployment target" below). No finished DFx module page is in `reference/`; the live site is the reference.
7. **Cpk convention:** teach the 2026 AIAG & VDA / ISO 22514 convention as current (Cp/Cpk and Pp/Ppk share the overall-sigma formula; the letter records whether stability was proven; the within-sigma index is Cw/Cwk). Teach the legacy 2005 AIAG convention explicitly as what supplier reports and Minitab-style software still print (Cpk from R̄/d₂, Ppk from overall s). Every page and calculator prints the sigma estimate beside every index; the capability calculator has a convention toggle. CLAUDE.md rule 3 is read in this light.
8. **Red bead experiment numbers (Module 2):** deming.org (S-E4, fetched directly this session) confirms the exercise's existence, its creation by Bill Boller of Hewlett-Packard as a 1982 gift to Deming, and its use on day one of Deming's four-day seminars, but does not state a box size, colour split, paddle-hole count or headcount for any specific session. Sources that do quote such numbers (4,000 beads, 800 red, a 50-hole paddle, 6 workers) are commercial training sites already on the S-E29 exclusion list ("not credible enough for historical or numerical claims"). Module 2 therefore presents a paddle of 50 holes and a 20 % red mix as the course's own constructed parameters for a common version of the exercise, explicitly labelled as such, rather than as Deming's documented figures. Apply the same standard to any future module that touches this experiment.

## Deployment target (recorded 2026-09-09)

- Portfolio: `https://erdemyilmaz.me/` (static site; `assets/css/site.css`, pages `index.html`, `about.html`). The home page lists the DFx course and says two more courses are "in preparation".
- DFx course: `https://erdemyilmaz.me/dfx/`. Layout: `index.html`, `modules/<NN-slug>.html` (e.g. `modules/00-introduction.html`), `glossary.html`, `references.html`, `about.html`, `assets/css/course.css`, `assets/js/course.js`. Navigation, module list, prev/next, quiz engine and progress are built by `course.js` from a `MODULES` array (`{num, slug, title, built}`); pages set `data-root` on `<body>` ("" at top level, "../" inside `modules/`). Header has "Back to erdemyilmaz.me"; footer has copyright, licence line, last-updated date. Progress stored in the browser only; a module counts as complete at a 70 % quiz score.
- `reference/course.css` and `reference/course.js` are byte-identical to the live DFx files apart from CRLF line endings. Phase C starts from them; the DFx course has no `calculators.html`, `tables.html`, or `formulas.html`, so those pages are new and must be added to the nav list in the copied `course.js`.
- Proposed path for this course: `https://erdemyilmaz.me/six-sigma/` (author to confirm the slug when deploying; nothing in the build depends on it because all links are relative).

## Environment (set up 2026-09-09)

- Python 3.11.9 (Windows Store build); installed numpy 2.4.6, scipy 1.17.1, pandas 3.0.5, statsmodels 0.15.0, pypdf.
- Node v24.19.0 and npm 11.17.0 installed via winget (OpenJS.NodeJS.LTS). New shells pick it up from PATH; an already-open shell may need its PATH refreshed.
- `CLAUDE.md` extracted from `sixsigmacourseclaudecodeprompt.md` (the source prompt file is kept in the folder).

## Log

### 2026-09-09, session 1: Phase A

- Confirmed the environment (above).
- Researched sources for all 20 modules. `SOURCES.md` lists 23 standards and manuals, 14 NIST handbook sections, 48 journal articles, 32 books, and 29 articles or web pages, each with status and reading depth. Every module has at least five verified sources.
- Read in full: NIST 6.1.6, 6.3.2.1, 6.3.2.2, 6.3.2.4, 2.4, 5.3.3, 7.2.2.2, 1.3.6.6.1, 8.1.6, 4.4.4; Wheeler 2019, 2015, 2011 columns; Nelson 1984; ASA 2016 p-value statement; Deming 1975; Juran 1975; GE 1998 annual report letter; NIST Baldrige Motorola 1988 profile; BusinessWeek 3M 2007; Fortune 2006 passage; Best and Neuhauser 2006; ISO 22514-1:2014 and ISO 7870-8:2017 scope previews; ASQ CSSGB BoK 2022; Deming Institute and Lean Enterprise Institute pages.
- Deviations from the brief: the 3M article is BusinessWeek, not the Wall Street Journal. The AIAG SPC manual has been replaced by the joint AIAG & VDA SPC Manual, 1st edition (July 2026); the course will cite that as current. ISO 22514-2 is now the 2026 (third) edition. Commercial training sites were excluded as sources.
- No HTML or verification code written (per the kickoff instructions).

### 2026-09-09, session 1 (continued): AIAG & VDA SPC Manual read

- Author placed a licensed, permission-protected PDF of the AIAG & VDA SPC Manual (1st ed., Feb 2026, 144 pp.) in `reference/`. Text extracted locally with pypdf to the session scratchpad (not into the repo); targeted sections read. SOURCES.md entry S-A2 upgraded to verified (full, targeted) with page references; S-A3 note updated; new controversy row and Part 4 items 1 and 1a added.
- Key finding: the manual changes the Cp/Cpk vs Pp/Ppk definition (see open decision 7). Other items now verifiable with page numbers: n ≥ 125 in 25 subgroups of 5; target tables by characteristic class; five stability rules; MSA as prerequisite; ISO 22514-2 time-dependent models; one-sided natural limits where Cpk may exceed Cp; outlier handling; tolerance limits never drawn on control charts.
- The FMEA PDF in `reference/` (libgen filename) is not used as a source.

### 2026-09-09, session 2: Phase B, verification harness

- `git init`; `.gitignore` excludes `reference/*.pdf` (licensed) and caches. First commit covers Phase A and Phase B.
- Built `verification/tables.py` (d₂, d₃ by numerical integration, c₄ from the gamma function, derived A₂ … E₂ for n = 2 to 25, rounded to published precision and checked against NIST 6.3.2.1, AIAG & VDA pp. 97–98 and Montgomery App. VI; Z, t, χ², F tables checked against NIST 1.3.6.7.x; scipy reference points for the JS tests; embeds the constants table into `stats.js`), `generate.py` (seeded registry, CSV plus JSON metadata with published expected values), `compute.py` (numpy, scipy, statsmodels route), `recompute.py` (plain-Python closed-form route with its own incomplete beta and gamma functions), `sanity.py` (relationship checks and the HTML cross-check convention: `<meta name="examples">`, `data-ex`/`data-key`/`data-dp`, `data-claim` with `data-min`/`data-max`, and a scan that every number with two or more decimals on a page is traceable), `assets/js/stats.js` (numerical functions and all calculator engines, UMD so Node can test it), `test_calculators.js`, `verify_all.sh`.
- Results: 8 textbook check examples, 63 published values matched (NIST 6.1.6, 6.3.2.2, 6.3.2.4, 7.2.2.2; AIAG & VDA p. 48 one-sided example with USL inferred as 0.40 mm; Montgomery plasma etch 2³ via UW STAT 502 notes; Minitab crossed gauge R&R ANOVA table). 686 quantities agree between the two Python routes; 1,896 values agree between Python and JavaScript; stats.js matches scipy to 1e-13 or better on 2,000+ points. Details in `EXAMPLES.md`.
- Deviations and notes: NIST D₄(5) is printed 2.115, exact rounds to 2.114 (course uses 2.114 and notes it). The individuals chart uses 3·MR̄/1.128, not the tabulated E₂ = 2.660, so that NIST's UCL 55.8041 is reproduced to four decimals. The AIAG MSA raw gauge R&R data set was not found in an open source; the ANOVA-table-to-variance-components step is checked against Minitab's published output, the raw-data step by three independent implementations. Capability results store `overall.*` (Pp/Ppk formula), `within.*` (Cw/Cwk, AIAG & VDA 2026) and `legacy.*` (2005 labels), plus a stability flag and the 2026 label the page should use; the quantile and z-score methods coincide with these for normal data and will be extended for non-normal data in Module 8.
- Environment note: Node is not on the Git Bash PATH; `verify_all.sh` finds `/c/Program Files/nodejs/node.exe`. Long heredocs fail in the Bash tool; write scripts to files instead.

### 2026-09-09, session 3: Phase C, scaffold and reference module

- `assets/css/course.css` and `assets/js/course.js` copied from `reference/` (identical to the live DFx files) and adapted: 20-module `MODULES` array (only Module 7 `built: true`), `PAGES` list with calculators (built), tables, formulas, glossary, references, about (planned, shown as "not yet published"), storage key `sixsigma-course-progress`, global `window.SIXSIGMA`. CSS additions for formulas, data tables, calculators and SVG charts (two-colour scheme, works in both themes; checked in dark mode).
- `assets/js/calculators.js`: shared SVG primitives (control chart with zones, per-point limits and frozen-baseline marker; histogram with spec limits, target and normal curve; Pareto bars) and all nine calculators (descriptive, capability with convention toggle, DPMO/sigma/RTY, control chart builder for I-MR, X̄-R, X̄-S, p, np, c, u with rule selection and frozen limits, crossed gauge R&R, t-tests and ANOVA with a correct p-value sentence, sample size, 2^k factorial, CLT and detection simulator with a seeded PRNG). Mount contract: `<div class="calc" data-calc="type"><script type="application/json" class="calc-data">…</script></div>`; `"collapsed": true` folds the inputs. Every calculator computes on load with its preloaded data.
- `index.html`, `calculators.html` (all nine calculators preloaded with verified datasets) and `modules/07-capability-1.html` (reference implementation: objectives, pre-check, why-this-matters, core content with two hand-drawn SVG figures, three worked examples plus the n = 30 CI example and the NIST check, calculator, eight common mistakes, two exercises with collapsible solutions and their own calculators, ten-question quiz, takeaways, thirteen references). About 6,700 words of prose excluding tables, quiz, figures and references (capability modules may run long per CLAUDE.md).
- Datasets registered: `m07-bore`, `m07-bore-30`, `m07-fill`, `m07-cpm`, `m07-ex1-keyway`, `m07-ex2-wall` (Module 7) and, for the calculators page, `m04-grr-bore`, `m11-ttest2-pull`, `m11-anova-machines`. Seeds were searched so that each dataset has its teaching property (stable with Cwk ≈ 1.06; unstable with a shift; %GRR ≈ 24 %; and so on); the search scripts stayed in the session scratchpad.
- Method for pages: data tables and calculator JSON are generated from the CSVs by a scratch script and substituted into a template, so no data value is typed by hand; the `allowed-numbers` meta lists conventions and hand-arithmetic intermediates, each computed by script before being typed. `sanity.py` now strips URLs before scanning a page for numbers (DOIs were false positives).
- Browser checks (local http.server, Chromium preview): no console errors on the three pages; the Module 7 calculator reproduces Table 2; quiz grades 10/10 and records progress; dark theme; narrow-screen layout. `.claude/launch.json` (ignored by git) starts `python -m http.server 8765` for the preview.
- Deviations: Module 7's "Why this matters" uses a labelled illustrative failure, since no verified public capability case with data was found; the PPAP acceptance bands remain cited through a secondary source. The Module 4 and 11 preload datasets were registered ahead of their modules (open decision 2).

### 2026-09-09, session 4: Phase D, Module 0

- Built `modules/00-introduction.html` (Introduction: what Six Sigma is and is not): objectives, why-this-matters on the Motorola 1988 Baldrige profile, seven sections (origin at Motorola and GE with the companies' claims labelled as such; sigma level as a number with the two-convention Table 1 and the 1.5σ shift controversy; DMAIC in one page with a gated-sequence figure; belts and roles from the ASQ BoK list; Lean and Six Sigma; the critiques with both sides and the quality of each piece of evidence; how to use the course), three worked examples, two calculators (descriptive histogram with specification limits; DPMO and sigma-level converter), seven common mistakes, two exercises with collapsible solutions and their own calculators, a ten-question quiz, seven takeaways, twenty references (three marked secondary or catalogue-only). About 4,700 words of prose in paragraphs and lists, above the 2,500 to 4,500 target; the critiques section and the two-convention explanation account for the excess and were kept.
- Sources re-read this session from the primary PDFs (NIST Motorola profile, GE 1998 letter, Fortune 2006, BusinessWeek 3M 2007, Schroeder 2008 abstract, ASQ CSSGB BoK map) so that every historical sentence matches the source wording; `SOURCES.md` S-E2 and S-E3 updated with the extra specifics. Claims not supported by a verified source were removed from the draft (the "first" Baldrige award year; the 1990s dating of the Lean and Six Sigma integration; the reason overproduction is the worst waste). The Bill Smith attribution, the trademark, and the MAIC-to-DMAIC evolution are cited as secondary.
- Harness extended with two parameter-only analysis kinds used by Module 0 and later by Module 6: `dpmo` (defects, units, opportunities → DPU, DPO, DPMO, Poisson first-time yield, Z with and without the 1.5 shift) and `sigma_table` (sigma level → PPM centred two-sided, shifted one-sided and two-sided; Z and sigma level of given PPM targets). Implemented in `compute.py`, `recompute.py` (closed form with its own normal functions), `sanity.py` (range, tail and monotonicity checks; the 3.4 PPM at 6σ known answer), `stats.js` (`S.dpmo` extended, `S.sigmaTable` added) and `test_calculators.js`. `verify_all.sh`: 40 examples, 1,285 quantities agree between the two Python routes, 3,386 values agree between Python and JavaScript, 159 page checks.
- Datasets registered: `m00-sigma-table`, `m00-bore-preview` (seed 37, chosen so that the I-MR chart is stable, Anderson-Darling p ≥ 0.10 and 4 to 6 of 100 are out of tolerance), `m00-dpmo-leak-1`, `m00-dpmo-leak-6`, `m00-ex1-dpmo`, `m00-ex2-pull` (seed 23, stable, exactly one value below the minimum). The seed-search and build scripts stayed in the session scratchpad; the build script asserts every hand intermediate against the results before writing the page.
- Browser checks (local http.server on port 8781, Chromium preview): no console errors; all four calculators compute on load with no NaN; navigation lists Module 0 and the pager shows only "Next"; quiz grades 10/10 and records progress; index shows 2 published modules; no horizontal scroll. Port 8765 was found occupied by an unrelated python server on this machine, so `.claude/launch.json` (git-ignored) now uses 8781.
- `course.js`: Module 00 `built: true`.

### 2026-09-09, session 4 (continued): Phase D, Module 1

- Built `modules/01-variation-statistics.html` (Variation and basic statistics): types of data; mean and median; range, variance and s with the n − 1 argument in words; histograms, quartiles and skewness; Anscombe's quartet as the real example (data printed, all four regressions recomputed, four-panel scatter figure drawn from the data by the build script); the normal distribution, the empirical rule and where the normal stops; lognormal (flatness example with log-scale statistics), Weibull, binomial and Poisson (the "clean sample of 50" example); sample, population and Deming's enumerative versus analytic distinction; the central limit theorem shown on the flatness data in subgroups of 2, 5 and 10 and by the simulator calculator. Four worked examples, three descriptive calculators plus the CLT simulator, eight common mistakes, two exercises, ten-question quiz, seven takeaways, twelve references. About 4,700 prose words in paragraphs and lists (above the 4,500 target by a similar margin to Module 0; the Anscombe and CLT sections were kept in full).
- Harness extended with three kinds: `descriptive` (statistics, histogram, optional count outside limits, optional statistics of ln x), `subgroup_means` (means of consecutive subgroups against s/√n, with skewness and Anderson-Darling p of the means) and `binomial_poisson` (pmf and cdf for a list of counts). `sd_pop`, `ss` and `sum` were added to every descriptive block so that by-hand arithmetic can be shown. `stats.js` gained `S.descriptiveBlock`, `S.subgroupMeans`, `S.binomPmf`, `S.poissonPmf`, `S.binomialPoisson`. The Module 1 build script introduced the `{{V:example:key:dp}}` placeholder so that no quoted statistic is rounded by hand.
- Datasets: `m01-shaft` (seed 21, chosen for a near-normal sample whose 12 subgroup means also behave), `m01-shaft-means`, `m01-flatness` (seed 10, lognormal; raw AD p ≈ 1e-9, log AD p 0.98; subgroup means lose their skew as predicted), `m01-flatness-means`, `m01-binomial`, `m01-ex1-torque` (seed 2), and the real `m01-anscombe-1` to `-4` with the published properties as known-answer checks (set 4's r is 0.8165; the tolerance on r is 0.001 and the page says so).
- Sources: NIST 1.3.6.6.8, 1.3.6.6.9, 1.3.6.6.18 and 1.3.6.6.19 read and added as S-B19 to S-B22; the Anscombe paper (S-C28) is available only as a scanned image, so the values were transcribed from Wikipedia (S-E35, secondary) and corroborated by the R datasets documentation (S-E36); the transcription is checked by reproducing every published property by script.
- `verify_all.sh`: 50 examples, 1,548 quantities agree between the two Python routes, 3,998 values agree between Python and JavaScript, 333 page checks. Browser checks on port 8781: no console errors, all four calculators compute on load with no NaN, quiz grades 10/10, no horizontal scroll.

### 2026-09-10, session 5: Phase D, Module 2

- Built `modules/02-process-thinking.html` (Process thinking): SIPOC and process maps (a SIPOC for the capstone brazed heat-exchanger line, no dedicated source found for the tool itself so it is presented as standard terminology, uncited, like a formula name); the eight wastes (Ohno's seven via S-E22/S-D12, Liker's eighth via S-D13 explicitly labelled as his addition); value stream mapping (term via S-D14, method via S-E23/S-D15) with a full worked current-state map (takt time, per-point inventory days, total lead time, process cycle efficiency) and a hand-drawn SVG figure; common cause versus special cause in words (Deming's enumerative/analytic distinction, S-C43); the red bead experiment (S-E4) and the funnel experiment (S-D10 p. 327 via S-E21) as the two demonstrations, both before any control chart. Four worked examples, one calculator (the existing descriptive-statistics calculator, reused and preloaded with the red-bead simulation — none of the nine required calculators fits value-stream or funnel arithmetic, so no new interactive calculator was built), eight common mistakes, two exercises (a second value-stream scenario, a shorter funnel run) with collapsible solutions, a ten-question quiz, seven takeaways, eleven references (four marked secondary, matching SOURCES.md's own bucketing for this module). About 4,200 words of prose in paragraphs and lists, above the 2,500–4,500 target by a similar margin to Modules 0 and 1.
- Sources verified fresh this session via WebFetch/WebSearch rather than assumed from memory: deming.org's red bead pages (confirms Bill Boller/Hewlett-Packard created the exercise as a 1982 gift, used on day one of Deming's four-day seminars, but no page states a box size, colour split, paddle-hole count or headcount for a specific session) and SPC for Excel's funnel-experiment page (confirms the exact wording of all four rules and the "variance doubles" / "41 % larger circle" result for Rule 2, matching this module's independent algebraic derivation). SOURCES.md S-E4 and S-E21 updated with these specifics; see PROGRESS.md decision 8 for why the red-bead box/paddle numbers are presented as the course's own constructed choice rather than Deming's documented figures (the sources that do quote such numbers are commercial sites already on the S-E29 exclusion list).
- Harness extended with two new analysis kinds, used only by Module 2 so far: `vsm` (value-stream lead time, takt time and process cycle efficiency from step cycle times and inventory quantities — simple arithmetic, no distribution) and `funnel` / `funnel_growth` (the four funnel-experiment rules as one-line recursions on a noise sequence; `funnel` is a single simulated path for the trajectory figure, `funnel_growth` runs 150 independent replications of a shorter simulation so that the Var(x_k) = k·σ² claim for Rules 3 and 4 can be checked against theory properly — a single random-walk path's own window variance turned out to be too noisy to verify tightly, which is a real statistical property, not a bug; the fix was replication, not a looser tolerance). Implemented in `compute.py`, `recompute.py` (plain-Python routes, no numpy), `sanity.py` (relationship and theory-comparison checks). No calculator or `stats.js`/`test_calculators.js` engine was built for these three kinds, since none of the nine required calculators covers this content; `test_calculators.js` now documents and explicitly skips kinds with no calculator (`SKIP_KIND`) rather than failing on "unknown kind".
- Datasets registered: `m02-redbead-theory` (Binomial(50, 0.20) probability table), `m02-redbead-days` (seed 42, 20 simulated draws), `m02-vsm-machining` and `m02-ex1-vsm` (fixed, non-random parameters for two value-stream scenarios), `m02-funnel` (seed 7, 100 drops, σ = 5.0 mm, illustrative single path) and `m02-ex2-funnel` (seed 19, 30 drops, σ = 4.0 mm, exercise), `m02-funnel-growth` (seed 7, 150 replications × 40 drops, σ = 5.0 mm, the Monte Carlo growth check). The build script asserted every hand intermediate against `results/` before it was typed, including one derived-but-unstored number (99.85 %, the non-value-add share = 100 − PCE), which was added to `allowed-numbers` after being computed by script.
- A CSS gap was found and fixed while checking the new SVG figures against `course.css` rather than trusting a screenshot alone: `chart-process` (used for the value-stream map's process-step boxes) was never a defined class, so those `<rect>` elements would have rendered with the SVG default black fill; and several `accent`/`muted`/`small` modifier spans were missing the `chart-text` base class that the compound selectors (`.chart-text.accent` etc.) require, so the modifiers would have silently done nothing. Fixed by adding `.chart-box` and `.chart-curve.bad` to `course.css` (extending the existing modifier pattern) and adding the missing base classes in the page. Lesson for future modules: grep every class used in a new SVG figure against `course.css`'s actual selectors before considering a figure done; a passing `sanity.py` and a clean console say nothing about whether a CSS class exists.
- `verify_all.sh`: 57 examples, 1,741 quantities agree between the two Python routes, 4,162 values agree between Python and JavaScript (52 examples with a calculator engine; the 3 new kinds have none, by design), 425 page checks. Browser checks on port 8781: no console errors; the reused descriptive-statistics calculator computes on load with no NaN; quiz grades 10/10 and records progress under key "02"; index page shows 3 of 4 published modules with a quiz passed; no horizontal scroll. The Browser pane reported itself "hidden" partway through this session (screenshots came back blank even though the DOM was fully correct) — `read_page`/`get_page_text`/`javascript_tool` were used instead, which is the documented behaviour, not a page bug.
- `course.js`: Module 02 `built: true`.

### 2026-09-10, session 6: Phase D, Module 3, and the batch plan for Modules 4–18

- The author asked to build Modules 3, 4, 5, 6, 9, 10, 12, 14, 15 and 18 in this one session, an explicit exception to the usual one-module-per-session rule (see the top of this file). Spent the start of the session re-reading `generate.py`, `compute.py`, `recompute.py`, `sanity.py`, `calculators.js`, `stats.js`, `course.js`, `course.css`, Module 2 in full, and `SOURCES.md` Parts 1 and 2 in full, then worked out which of the ten modules need genuinely new verification "kinds" versus pure reuse of what Phase B/C/D already built. Recorded the result as "New harness kinds needed, by module" above so a fresh session does not have to redo this analysis; three of the ten (5, 15's allocation half, 18) need zero new kinds, several need only a small arithmetic addition, and Modules 4, 9, 12 and 15 need real new statistical machinery (attribute agreement/kappa, a simplified multi-vari decomposition, multiple regression, and a tolerance-stack Monte Carlo).
- Built `modules/03-defining-a-project.html` (Defining a project): problem statements, project charters (business case, problem statement, goal statement, scope and boundaries, timeline and team), VOC to CTQ (Griffin and Hauser's structure), the Kano model (with Kano's own TV/clock validation), stakeholder power/interest analysis, and what a good Green Belt project looks like, illustrated with Sharma and Rao's 2014 open-access published crankshaft DMAIC case (sd 0.003 to 0.002 mm, Cp 1.29 to 2.02, Cpk 0.32 to 1.45) quoted exactly as published and explicitly flagged as not independently recomputed, since it is real data, not a constructed example. Three hand-drawn SVG figures (a CTQ tree, the Kano diagram, a stakeholder power/interest grid). Three worked examples, one calculator (the existing DPMO/sigma-level converter, preloaded with the charter's own baseline — no new calculator needed), eight common mistakes, two exercises with collapsible solutions, a ten-question quiz, seven takeaways, eight references (6 verified, 2 secondary, matching `SOURCES.md`'s count for this module exactly). About 3,900 words of prose.
- No new harness kind was needed: the charter's baseline (42 of 1,000 brazed assemblies fail the leak test, 4.2&nbsp;%) and goal (10 of 1,000, 1.0&nbsp;%) both reuse the existing `dpmo` kind with `opportunities: 1`, chosen deliberately to match the capstone's own stated baseline (SOURCES.md Module 19) so the two tie together. Datasets registered: `m03-charter-leak`, `m03-charter-goal`.
- Browser checks (`.claude/launch.json` changed to port **8782** this session — port 8781 was occupied by another chat's already-running server for the same static site; both are plain `python -m http.server` on the same files, so running a second one on a different port is harmless and nothing needed to be stopped): no console errors; all three new SVG figures render with the expected element counts; the DPMO calculator computes on load with no NaN and matches the page's own numbers exactly; quiz grades 10/10 via `window.SIXSIGMA.gradeQuiz`; no horizontal overflow (scrollWidth 1265 vs innerWidth 1280).
- `verify_all.sh`: 65 examples, 2,171 quantities agree between Check 1 and Check 2, 4,494 values agree between Check 1 and the JavaScript engines, 435 page checks. `course.js`: Module 03 `built: true`.

### 2026-09-09, parallel session: Phase D, Module 8

- The author asked for Modules 8, 11, 13, 16, 17 and 19 in one session (the strongest-model modules) while another session builds the rest; see the parallel-session note at the top.
- Built `modules/08-capability-2.html` (Process capability II): objectives, pre-check, why-this-matters on the stability and non-normality controversies with both sides cited (AIAG & VDA 2026 pp. 38, 45–50, 67–75; NIST 6.1.6; ISO 22514-2:2026; Wheeler 2019 and 2011 Parts One and Two), four worked examples (tool-wear drift with Cwk 1.37 vs Ppk 0.97; flatness with four capability answers on one dataset; leak-test attribute capability with Wilson and exact intervals; a 30-part supplier report taken apart with six questions), the ISO general geometric and z-score methods with formulas, four routes to non-normal quantiles with the limitation of each (Clements described but not computed), one-sided and bounded characteristics, nine misuses, eight common mistakes, two exercises with collapsible solutions (Rayleigh-type runout where the lognormal is rejected and Box-Cox λ 0.4 accepted; attribute rejects with a sample-size plan), ten-question quiz, eight takeaways, thirteen references. Seven calculators on the page (capability ×2, descriptive ×2, p chart ×2, DPMO). About 5,900 words of prose (capability modules may run long).
- Harness: two new kinds, `capability_nonnormal` (naive normal, fitted lognormal with the ISO/AIAG-VDA G and Z indices, Box-Cox with the MLE λ rounded to two decimals for use, NIST empirical percentiles, I-MR charts on x and ln x) and `attribute_capability` (p chart, Wilson and Clopper-Pearson intervals, DPMO, Z, sample size for a stated precision), in `compute.py` (scipy), `recompute.py` (golden-section Box-Cox likelihood; bisection on the regularised incomplete beta for the exact interval) and `sanity.py`; both are static page computations with no calculator, so `test_calculators.js` skips them (SKIP_KIND, documented). `recompute.close()` compares `lambda_mle` at 1e-5 because two optimisers of a flat likelihood agree only to its resolution; everything downstream uses the rounded λ, compared exactly.
- Datasets: `m08-drift` (seed 2), `m08-flatness` (the Module 1 rows), `m08-leak` (seed 1), `m08-supplier` (seed 30), `m08-ex1-runout` (seed 1), `m08-ex2-paint` (seed 1); seed searches in the session scratchpad. Page built from a template by a scratchpad builder (`build.py` + `m08_inter.py`): data tables, calculator JSON and every hand intermediate generated from `results/`; 57 intermediates auto-listed in `allowed-numbers`.
- Sources: S-B20 (NIST 7.2.4.1) added; S-E7 extended to Wheeler's Part Two, read in full. Champ and Woodall's 1987 ARL table (for Module 16) was not found in any open reproduction; Module 16 will compute the ARLs by its own Markov chain and cite the paper for the method.
- Browser (port 8791): no console errors; all seven calculators compute on load; the drift example is labelled Pp/Ppk by the calculator verdict and the supplier example Cp/Cpk; quiz 10/10 records progress. `verify_all.sh`: 65 examples, 2,171 quantities agree between Check 1 and Check 2, 4,494 values agree between Check 1 and JavaScript, 571 page checks.
- Fixed a scaffold CSS issue found at 375 px: the calculator key/value grid (`.kv`) forced a horizontal page scroll; now wraps.
- Deviations: about 5,900 words of prose; Clements' method described and cited but not computed (its Pearson-curve tables have no open reproducible source, and Wheeler's critique of the input statistics is taught instead).

### 2026-09-10, parallel session: Phase D, Module 11

- Built `modules/11-hypothesis-testing.html` (Hypothesis testing): objectives, pre-check, why-this-matters on the ASA 2016 p-value statement (six principles paraphrased) plus an illustrative underpowered before/after comparison whose two-proportion arithmetic is computed by script; the logic of a test with a Type I/II figure; p-values and confidence intervals read correctly; Cohen's d with its behavioural-science origin flagged; six worked examples (Welch two-sample t on the pull-strength suppliers with per-group AD and Levene; paired t on cycle times with the unpaired contrast; one-sample t on torque, significant but small; one-way ANOVA on three machines with the full table, η² and the Tukey caveat; chi-square on defect type by shift with expected counts, contributions and Cramér's V; Mann-Whitney on skewed flatness against Welch on the same data, with the Hodges-Lehmann shift); tests for variances (F vs Levene); normality tests and their limits; power and sample size with the NIST published check and the pull study's power (0.46 at n = 12; 29 per group for 80 %); eight common mistakes; two exercises with collapsible solutions; ten-question quiz; seven takeaways; fourteen references. Five calculators (tests ×4, sample size). About 5,300 words of prose.
- Harness: three new kinds, `chisq` (contingency table, expected counts, contributions, Cramér's V, critical value), `mannwhitney` (average ranks with ties, U, tie-corrected σ, continuity-corrected z as scipy's asymptotic method, Hodges-Lehmann, probability of superiority, Welch for comparison) and `power` (z-approximation power at a given n with a curve; noncentral-t power as information only), in `compute.py`, `recompute.py` (own ranking and chi-square tail) and `sanity.py`; no calculator, so `test_calculators.js` SKIP_KIND extended and documented. Builder gained an `rtable` placeholder (rows of result arrays as data-ex spans) used for the power curve table.
- Datasets: `m11-pull-a/b` (the existing pull groups), `m11-paired-cycle` (seed 6), `m11-ttest1-torque` (the Module 1 torque rows), `m11-chisq-shift` (seed 1, multinomial), `m11-mw-flatness` (seed 39), `m11-power-pull`, `m11-samplesize-pull`, `m11-ex1-cure` (seed 2), `m11-ex2-chisq-leak` (seed 1); `m11-ttest2-pull`, `m11-anova-machines` and `chk-nist-samplesize` reused and declared.
- Sources added (catalogue or abstract depth, verified by search 2026-09-10): Pearson 1900, Hodges and Lehmann 1963, Cramér 1946, D'Agostino and Stephens 1986.
- Browser (port 8791): no console errors; all five calculators compute on load with the correct p-value sentence; quiz 10/10. `verify_all.sh`: 75 examples, 2,297 quantities agree between Check 1 and Check 2, 4,632 values agree between Check 1 and JavaScript, 720 page checks.
- Deviations: none of substance. Tukey's multiple-comparison procedure is described and pointed at software rather than computed (no harness kind); the two-proportion test is described here and worked in Module 8's attribute capability arithmetic.

### 2026-09-10, parallel session: Phase D, Module 13

- Built `modules/13-design-of-experiments.html` (Design of experiments): objectives, pre-check, why-this-matters on the history (Fisher, Yates, Plackett-Burman, Box-Wilson) and the Taguchi debate as the real example plus an illustrative OFAT failure; OFAT worked example with the interaction plot figure; the 2^k design with the sign table, contrast/effect/SS/standard-error formulas; a fully worked 2³ × 2 (effects by hand from cell means, full ANOVA table, F against its critical value, standard error of an effect, two-way table for the interaction, prediction equation and corner predictions); randomisation, replication, blocking (ABC confounded with day) and centre points; fractional factorials with generator, defining relation, aliases and resolution, a worked 2⁴⁻¹ with Lenth's method by hand and the alias caveat; the Montgomery plasma-etch published check; response surface introduction with a schematic figure; Taguchi in three parts with the Box 1988 and Nair 1992 positions; eight common mistakes; two exercises with collapsible solutions; ten-question quiz; seven takeaways; fifteen references. Four factorial calculators. About 4,700 words of prose.
- Harness: Lenth's method (s₀, PSE, m/3 degrees of freedom, ME, SME, active list) added to the `factorial` kind for unreplicated designs in `compute.py`, `recompute.py` and `stats.js` (all three routes agree; `sanity.py` checks the ME formula and the active list); the factorial calculator now prints the Lenth table and uses ME as the Pareto reference line when there is no residual. `sanity.py` page scan now recognises a negative value printed with the typographic minus sign.
- Datasets: `m13-ofat` (seed 1), `m13-mould` (seed 12), `m13-plating` (seed 1, D = ABC column stored in the CSV for display), `m13-ex1-weld` (seed 2), `m13-ex2-roughness` (seed 1); `chk-montgomery-etch` declared as the published check.
- Sources added: NIST 5.3.3.4 (with 5.3.3.4.1, 5.3.3.4.4, 5.3.3.4.5), 5.3.3.6, 5.5.6 read in full; Lenth 1989 (abstract).
- Browser (port 8791): no console errors; four calculators compute on load; quiz 10/10. `verify_all.sh`: 80 examples, 2,503 quantities agree between Check 1 and Check 2, 4,856 values agree between Check 1 and JavaScript, 813 page checks.
- Deviations: the planned 2⁵⁻¹ example became a 2⁴⁻¹ (8 runs) so the existing k ≤ 3 engine and the calculator carry it; the response surface section is descriptive (no RSM harness kind), as the curriculum asks for an introduction only.

### 2026-09-10, parallel session: Phase D, Module 16

- Built `modules/16-spc-1.html` (Statistical process control I): objectives, pre-check, why-this-matters on Shewhart's 1924 memo and 1931 book (Best and Neuhauser; Shewhart) with Woodall 2000 on the live controversies, plus an illustrative specification-chart failure; common and special causes as an operational rule; control limits vs specification limits (second of three treatments) with a table and a figure; why three sigma; X̄-R (the Module 7 bore), X̄-S (n = 10) and I-MR (furnace, with a special cause excluded and the limits recomputed) built by hand with the constants; the NIST individuals check; the Western Electric rules and Nelson's eight tests stated, with the AIAG & VDA stability criteria; a worked 1σ shift that rule 1 misses and the runs rules catch, including a Nelson-4 false alarm; an ARL table computed by the course's own Markov chain for five rule sets and six shifts with the √n scaling for X̄ charts; how often to sample and rational subgroups (Wheeler; AIAG & VDA p. 128); eight common mistakes; two exercises (X̄-R from scratch; a trend on an I-MR chart with the σ̂ vs s diagnostic); ten-question quiz; seven takeaways; thirteen references. Calculators: control chart builder ×4 (bore X̄-R with rule selection and freeze; the shift and both exercises collapsed) and the detection simulator. About 5,600 words of prose (SPC modules may run long).
- Harness: new kind `arl` — `compute.py` builds the Champ-Woodall Markov chain over the zone categories of the last four observations plus a same-side run counter (about 1,500 to 2,000 states per rule set) and solves it sparsely; `recompute.py` simulates run lengths with its own xorshift generator (20,000 in-control and 4,000 shifted runs per rule set) and `close()` compares `arl.*` at 5 %; `sanity.py` checks the rule-1 column against 1/p, the 370.4 in-control value, monotonic decrease with shift, and that added rules never lengthen the run. `test_calculators.js` SKIP_KIND documented. Result: the chain reproduces the classic in-control values 370.40, 225.44, 166.05, 152.73 and 91.75 exactly, so the Champ and Woodall figures the course could not find reproduced in an open source are now verified by computation.
- Datasets: `m16-seal` (seed 1), `m16-furnace` (seed 25) and `m16-furnace-clean`, `m16-shift` (seed 585), `m16-arl`, `m16-ex1-pin` (seed 1), `m16-ex2-trend` (seed 1270, chosen so that the run rules fire before rule 1 and the MR chart stays quiet); `m07-bore` and `chk-nist-imr` declared.
- Sources: AIAG & VDA manual pages 41–42 (stability criteria, rule sets, OC and ARL), 80, 83 and 128 read this session and cited; no new external sources.
- Browser (port 8791): no console errors; five calculators compute on load; quiz 10/10. `verify_all.sh`: 87 examples, all four checks pass, 905 page checks.
- Deviations: none of substance. The m16-shift limits are computed from all 50 values (the text says so and the calculator's freeze option shows the baseline alternative).

### 2026-09-10, parallel session: Phase D, Module 17

- Built `modules/17-spc-2.html` (Statistical process control II): objectives, pre-check, why-this-matters with two documented disagreements (AIAG & VDA p. 90 "Shewhart charts require normal data" vs Wheeler; binomial limits vs Laney) plus an illustrative over-dispersed p chart failure; attribute charts (p, np, c, u) with formulas, the manual's np̂(1 − p̂) ≥ 9 and 25 % sample-size notes, and a worked p chart catching a supplier change (limits computed from all lots, with the freeze-the-baseline point); a c chart with one special cause and the np and u limits; over-dispersion and Laney's p′ with the z-score, σ_z and limit formulas worked on a 25-day high-volume example; a chart selection tree figure; EWMA and CUSUM formulas with NIST's and the manual's parameter guidance, a worked 0.7σ drift on three charts (I chart misses, both memory charts signal at reading 38, CUSUM's last zero locating the change) and the NIST EWMA published check; short-run DNOM chart with the pooling assumption checked by the per-part sd ratio; the flatness data raw and on a log scale read against both positions; a seven-step reaction sequence and the out-of-control-but-in-spec case (third treatment of the limits question); eight common mistakes; two exercises (u chart with a special cause; 0.5σ shift on I-MR vs EWMA); ten-question quiz; eight takeaways; sixteen references. Four control-chart-builder calculators. About 6,000 words of prose (SPC modules may run long).
- Harness: two new kinds, `p_prime` (Laney: classic p chart plus z-scores, MR̄(z)/1.128, scaled limits, runs rules on z) and `dnom` (I-MR of x − nominal with a per-part summary and sd ratio), in `compute.py`, `recompute.py` and `sanity.py`; `test_calculators.js` SKIP_KIND extended and documented. The existing `ewma`, `cusum`, `p`, `np`, `c`, `u` and `imr` kinds carry the rest.
- Datasets: `m17-p-leak` (seed 72), `m17-np-connector` (1), `m17-c-porosity` (3), `m17-u-solder` (1), `m17-pprime` (1), `m17-drift-imr/-ewma/-cusum` (7), `m17-dnom` (7), `m17-flatness-imr/-log-imr` (the Module 1 rows), `m17-ex1-u-braze` (13), `m17-ex2-imr/-ewma` (1); `chk-nist-ewma` declared.
- Sources added: Arafah 2022 (open access, read in full for the Laney formulas) and Minitab's Laney methods page (partial); AIAG & VDA pp. 88–92 and 111–118 read and cited.
- Browser (port 8791): no console errors; four calculators compute on load; quiz 10/10. `verify_all.sh`: 101 examples, all four checks pass, 973 page checks.
- Deviations: the EWMA and CUSUM charts are hand computations on the page (no calculator draws them; the builder handles the Shewhart charts), consistent with the curriculum's "introduction" scope; Lucas and Saccucci's L values for small λ are described qualitatively because only the abstract was read.

### 2026-09-10, parallel session: Phase D, Module 19 (capstone)

- Built `modules/19-capstone.html` (Capstone: a DMAIC project end to end): a brazed aluminium heat-exchanger line with a 4.3 % leak-test reject rate, worked in ten steps across Define (charter from Module 3, SIPOC, CTQ, stakeholders), Measure (gauge R&R on the gap gauge; baseline p chart and attribute capability with DPMO and the n-for-precision; Pareto of leak locations; gap capability with the X̄-R chart first and the reconciliation of Cpk 0.89 with a 4 % leak rate through the 0.14 mm bridging limit; furnace I-MR), Analyze (cause-and-effect branches; an FMEA excerpt with action-priority logic; sectioned leakers vs passers by Welch's t with the association-vs-causation point), Improve (2³ × 2 DOE with the interaction read from the two-way table, settings chosen to change one thing, prediction reproducing the baseline cell; pilot; confirmation capability Cpk 1.79 and leak rate 0.81 % with intervals; 2 × 2 chi-square with the difference interval; honest wording where the goal is met at the point estimate but not at the top of the interval) and Control (control plan table, reaction plan, ongoing monitoring with frozen limits and monthly Cpk intervals, closure with finance owning the benefit). Every task has a collapsible solution; seven traps; ten-question quiz; six takeaways; nine references (S-C46 as the real published case). Eight calculators (gauge R&R, p chart ×2, DPMO, capability ×2, tests, factorial). About 4,700 words of prose plus solutions.
- Harness: one new kind, `pareto` (sorted counts, percentages, cumulative, top-80 % count) in `compute.py`, `recompute.py`, `sanity.py`; SKIP_KIND documented. All other capstone datasets reuse existing kinds (attribute_capability, grr, capability, imr, ttest2, factorial, chisq).
- Datasets: the `m19-*` family (seeds 1, 4, 2, 4, 2, 9, 2, 1 per generate.py) built in one block so that totals agree (the Pareto sums to the baseline leakers; the 2 × 2 table is the two attribute studies' totals; the DOE's production cell reproduces the baseline gap mean; the after data sit at the DOE's chosen cell). `m03-charter-leak` and `m03-charter-goal` declared for the charter numbers.
- Browser (port 8791): no console errors; eight calculators compute on load; quiz 10/10. `verify_all.sh`: 111 examples, all four checks pass, 1,084 page checks.
- Deviations: the FMEA excerpt's ratings are stated as judgements (Module 10 owns the method); the financial benefit is deliberately not computed (consistent with Module 0's critique). Course-wide status after this session: Modules 0, 1, 2, 3, 7, 8, 11, 13, 16, 17, 19 built; `course.js` marks all of them built.

### 2026-09-10, session 6 (continued): Phase D, Module 4

- Built `modules/04-measurement-systems.html` (Measurement systems analysis): why the gauge comes before the process (NIST 2.4/2.4.4 and Burdick, Borror and Montgomery 2003 on the variance-decomposition argument, an illustrative gauge-explains-the-apparent-drift failure); bias, linearity and stability studies; repeatability vs reproducibility in words; crossed gauge R&R by the ANOVA method with the full formula set (%Contribution, %Study Variation, %Tolerance, ndc) and the AIAG acceptance bands cited as guidelines; attribute agreement analysis with Cohen's and Fleiss' kappa and the Landis and Koch (1977) benchmark scale (verified this session via WebSearch/a PubMed fetch that hit a cookie wall, so cited as secondary — the benchmark numbers are corroborated by multiple citation-listing sources, not read from the original paywalled paper); what to do with a bad gauge, mapped fix-by-failure-mode. Four worked examples (bias+linearity on the bore gauge; stability on the same reference; a two-gauge %GRR comparison, bore vs a checkweigher, with the gauge R&R calculator preloaded; attribute agreement on braze fillet appearance with a full 30-part table), two exercises with collapsible solutions, ten-question quiz, seven takeaways, nine references (7 verified, 2 secondary, matching `SOURCES.md`'s count for this module). About 4,400 words of prose.
- Harness: one new kind, `attribute_agreement` (Fleiss' kappa across appraisers, Cohen's kappa per appraiser vs a known standard, within-appraiser repeatability, overall and per-appraiser raw effectiveness). `compute.py`'s route uses pandas pivot/groupby; `recompute.py`'s route is plain dict lookups and loops with no pandas, a genuinely different implementation of the same formulas — both flatten every nested result (`fleiss.kappa`, `cohen_kappa_vs_standard.A.kappa`, etc.) to dotted-path keys, matching the rest of `recompute.py`'s convention (the first attempt returned nested dicts from `r_attribute_agreement` and crashed `close()`, which subtracts numbers, not dicts — fixed by flattening). `sanity.py` checks every kappa is in [-1, 1], every effectiveness/repeatability figure in [0, 1], Fleiss' p0 + p1 = 1, and that the reported kappa matches the (P̄ − P̄ₑ)/(1 − P̄ₑ) formula from the stored intermediates. `test_calculators.js`'s `SKIP_KIND` extended (no calculator among the required nine fits attribute agreement, same precedent as Module 2's `vsm`/`funnel`). One bug caught by the harness itself and fixed before committing: `generate.py` writes every CSV field through `repr()` for floats, so an integer "part" id round-trips through `compute.py`'s CSV loader as the float `1.0`; both `attribute_agreement()` and `r_attribute_agreement()` now convert with `str(int(v)) if isinstance(v, float) else str(v)`, the same pattern already used by the existing `grr()` function for its own "part" column — worth remembering for any future kind that treats a numeric-looking column as a categorical id.
- Datasets: `m04-grr-scale` (a checkweigher, %GRR about 7 % of study variation — the "good gauge" contrast to the already-registered `m04-grr-bore`'s conditional 24 %), `m04-bias`, `m04-linearity`, `m04-stability` (all three on the same 12.010 mm reference standard and the bore gauge, so the bias found in the first study is still present, unchanged, in the stability study — the gauge is consistently, stably biased, which is the point), `m04-attribute-braze` (3 inspectors × 30 parts × 2 trials, 6 deliberately borderline, seed chosen for Fleiss' κ ≈ 0.6), `m04-ex1-bias`, `m04-ex2-attribute` (exercises). The charter baseline/goal figures from Module 3 (`m03-charter-leak/-goal`) were not re-touched.
- **Working-tree note:** by the time this module's verification was run, the parallel session had committed Module 19, and its `generate.py` diff turned out to include this session's already-written `m04-grr-scale`/`m04-bias`/`m04-linearity`/`m04-stability`/`m04-attribute-braze` registrations along with the m19 ones (see the collaboration note above). `compute.py`/`recompute.py`/`sanity.py`/`test_calculators.js`'s `attribute_agreement` additions were not affected and this session's commit for Module 4 carries them plus the two exercise datasets, the page, and the tracking-file updates; the net result on disk and in `EXAMPLES.md` is unaffected, only which commit each hunk lives in.
- Browser (port 8781 — `.claude/launch.json`'s `"static"` config, port renumbered again this session since the parallel session's server was no longer running on it): no console errors; all three new SVG-free figures are plain tables so nothing to check there; the gauge R&R calculator computes on load with no NaN and reproduces the bore-gauge study; quiz grades 10/10 via `window.SIXSIGMA.gradeQuiz`. (A `document.documentElement.clientWidth`-based horizontal-scroll check returned 0/nonsensical values with the Browser pane unfocused, consistent with the documented "pane reports itself hidden" quirk extending beyond screenshots to some layout measurements — not trusted, not treated as a real finding; the `.table-wrap`/`table.data` CSS pattern used for every wide table here is unchanged from the one already proven across Modules 0–3.)
- `verify_all.sh` (both sessions' work combined): 118 examples, 3,315 quantities agree between Check 1 and Check 2, 7,330 values agree between Check 1 and the JavaScript engines, 1,126 page checks. `course.js`: Module 04 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 5

- Built `modules/05-data-collection.html` (Data collection and sampling): operational definitions (Deming, tied back to Module 4's attribute agreement study as a direct test of one); sampling plans; rational subgrouping (Wheeler, S-E6) as the section the module is really about; data collection sheets; the three named data quality failures (rounding to the tolerance, mixed streams, sorting before measuring). No fully verified real-world case exists for this module (SOURCES.md Part 4 gap note 3), so "why this matters" leads with a labelled constructed illustration instead. Three worked examples, seven common mistakes, two exercises with collapsible solutions, ten-question quiz, seven takeaways, seven references (5 verified, 2 secondary, matching `SOURCES.md`'s count). About 4,000 words of prose.
- The centrepiece worked example turned out richer than planned: subgrouping the identical 100 values by production order (naive, mixing two mould cavities in every subgroup) versus by cavity (rational) doesn't just change R̄ — the rational grouping's primary 3-sigma rule only catches 3 of 20 points, but the supplementary run rules (WE4 "8 in a row on one side," WE2, WE3) unambiguously catch the full pattern, which is a genuine, better argument for teaching multiple rules than anything invented for the purpose. Checked this by pulling the actual `chart.rules_x` object from `results/m05-subgroup-rational.json` rather than assuming the shift would show as "every point in the second half beyond the limits" (it does not: the limits are still centred on the *grand* mean of both cavities, which sits between the two, and the primary rule only fires on the few points from either cavity that swing far enough past it by chance) — worth remembering for any future module that invents a similar shift-detection example: don't narrate the expected mechanism without checking the actual per-point rule output.
- No new harness kind: rational subgrouping reuses `capability` (`chart: "xbar_r"`) on the same 100 (or 40, for the exercise) values with two different subgroup-index columns; rounding loss reuses `descriptive` on the same values rounded two ways. Two control-chart-builder calculator instances (naive and rational) and one descriptive-statistics instance are the first reuse of those calculators for a non-capability, non-MSA teaching point.
- Datasets: `m05-subgroup-naive`/`m05-subgroup-rational` (two-cavity mould, seed 1, 100 individuals, same values reordered), `m05-rounding-fine`/`m05-rounding-coarse` (80 individuals, seed 1, resolution 0.001 vs 0.01 mm, ~13 % sd inflation), `m05-ex1-naive`/`m05-ex1-rational` (two-lathe exercise, seed 5, 40 individuals). No dataset needed for the sampling-plan worked example or the second exercise (both qualitative).
- **A real error caught and fixed before committing:** the descriptive-statistics calculator on the rounding-loss example was first preloaded with a hand-typed array (mean 6.5001, s 0.00436) that did not match the registered `m05-rounding-coarse` dataset (mean 6.4998, s 0.00763) — a transcription slip when constructing the calculator's JSON by hand instead of extracting it from the CSV. Caught by comparing the calculator's live output against the page's own stated numbers during the browser check, not by any automated script (`sanity.py` cannot see inside a `<script class="calc-data">` JSON blob, only `data-ex` spans). Fixed, and separately re-verified programmatically that the two control-chart calculators' embedded arrays and the two hand-built HTML data tables (Table 1, Table 2 — 100 values each) exactly match their source CSVs, since those were also transcribed by hand from a script's printed output rather than generated by a template. **Lesson for future modules:** any raw data embedded in a calculator's `calc-data` JSON or in an HTML table needs to be extracted programmatically (e.g. `json.dumps` straight from the CSV) and, ideally, diffed against the CSV after pasting into the page, not eyeballed — `sanity.py`'s traceability check only covers `data-ex` spans and does not, and cannot, validate a calculator's preloaded dataset or a plain data table against the source CSV.
- Browser (port 8781): no console errors; both control-chart calculators compute on load with no NaN and reproduce the R̄ values quoted in the text; quiz grades 10/10. `verify_all.sh`: 124 examples, 3,599 quantities agree between Check 1 and Check 2, 7,969 values agree between Check 1 and the JavaScript engines, 1,141 page checks. `course.js`: Module 05 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 6

- Built `modules/06-sigma-level-dpmo.html` (Sigma level, DPMO, DPU, RTY): defects vs defectives; opportunities and how counting them gets gamed (a same-data, three-opportunity-count comparison showing DPMO swing 10× and sigma level rise nearly a full point with zero real process change); DPU/DPO/DPMO formulas; FTY vs RTY with the "hidden factory" framing; sigma-level conversion; the 1.5σ shift's history (Motorola's own convention) and controversy in depth (Tadikamalla's definitional critique, Bothe's small-subgroup statistical rationale, Burns' not-in-control objection), going deeper than Module 0's preview of the same topic without re-deriving what Module 0 already covered. Three worked examples, one calculator (the existing DPMO/sigma-level/RTY converter, preloaded with the connector and the 5-step line), seven common mistakes, two exercises with collapsible solutions, ten-question quiz, six takeaways, eight references (6 verified, 2 secondary, matching `SOURCES.md`'s count). About 3,300 words of prose.
- New kind `rty_chain` (params `yields` → RTY = product, normalised yield = RTY^(1/k), total DPU = −ln(RTY), cumulative RTY). Unlike every other new kind added this session, this one **has** a calculator: `Calc.dpmo`'s section C already presented RTY arithmetic inline in `calculators.js` (not through a `stats.js` function `test_calculators.js` could test). Rather than skip it in `SKIP_KIND` as usual, extracted the inline math into `S.rtyChain(params)` in `stats.js` (also now returning `cumulative_rty`, which the calculator displays as a new small table), pointed `Calc.dpmo` at the shared function instead of duplicating the arithmetic, and wired `"rty_chain"` into `test_calculators.js`'s kind dispatch so Check 4 genuinely covers it. `recompute.py`'s independent route computes RTY via `exp(sum(log(y)))` rather than compute.py's running product — a different enough numerical path to be a real second implementation despite the underlying triviality of the arithmetic. `dpmo` and `sigma_table` needed no changes, only new registrations.
- Datasets: `m06-dpmo-connector` (54/2,400/5, DPMO 4,500), `m06-gaming-op1`/`m06-gaming-op10` (the identical 54/2,400 recounted at 1 and 10 opportunities), `m06-rty-chain` (5-step line, yields matching `Calc.dpmo`'s own client-side default `[0.98, 0.95, 0.99, 0.97, 0.985]` so the calculator's out-of-the-box view matches the page), `m06-sigma-table` (half-sigma steps 1 to 6, both conventions, the 3.4 PPM Motorola target re-checked as a published value at k=6), `m06-ex1-rty`, `m06-ex2-dpmo` (exercises).
- Browser (port 8781): no console errors; the DPMO calculator's three sections (defect count, conversion, RTY chain) all compute on load with no NaN and reproduce the page's own Table 1/2 numbers exactly; quiz grades 10/10. `verify_all.sh`: 131 examples, 3,689 quantities agree between Check 1 and Check 2, 8,106 values agree between Check 1 and the JavaScript engines, 1,178 page checks (after fixing one `allowed-numbers` gap for three hand-computed percentage restatements — 88.06/92.67/98.12 — of stored fraction fields). `course.js`: Module 06 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 9

- Built `modules/09-graphical-analysis.html` (Graphical analysis): why look before you test (Juran's own correction of the Pareto-principle attribution; Anscombe's quartet revisited from Module 1 with a graphical-analysis framing rather than re-derived); Pareto charts; box plots (Tukey); scatter plots; stratification (explicitly tied back to Module 5's rational-subgrouping lesson); multi-vari charts (Seder) as the module's centrepiece; time series plots as the direct precursor to Modules 16/17's control charts. Three worked examples (a Pareto of leak-test causes with a hand-drawn SVG figure; a full multi-vari chart on braze fillet width, temporal variation dominant at 65% of σ̂, tied to a furnace-warm-up story; a box-plot-by-shift comparison with a hand-drawn SVG figure showing shift 3 running higher, wider, and over the USL on its worst reading), seven common mistakes, two exercises with collapsible solutions (a second Pareto; a second multi-vari with positional variation dominant instead, a deliberate contrast to the main example), ten-question quiz, six takeaways, seven references (6 source IDs represented, S-C47 and S-E27 combined into one citation since they are the same underlying ASQ page). About 3,600 words of prose.
- New kind `multivari`: a simplified, range-based (R̄/d₂-style) decomposition into positional (within-part), cyclical (part-to-part within a time point) and temporal (time-to-time) σ̂ components, deliberately not a formal ANOVA, because Module 9 is graphical/exploratory and precedes Module 11's hypothesis testing. `compute.py`'s route uses pandas groupby; `recompute.py`'s route is plain dict grouping and loops. Discovered mid-session that `pareto` did **not** need to be built new as originally planned in "New harness kinds needed" below — the parallel session had already added it for Module 19 (columns `category`, `count`) — so Module 9 needed only one new kind, not two; updated the plan note accordingly rather than leaving it to mislead a future session.
- Datasets: `m09-pareto-leak` (169 rejects, 6 causes), `m09-multivari-braze` (5×4×3 design, seed chosen for temporal variation to dominate at roughly two-thirds of σ̂, consistent with a furnace-warm-up story), `m09-boxplot-shift1/2/3` (bore diameter, 30 each, seed chosen so shift 3 runs higher, wider, and its single worst reading exceeds the specification limit), `m09-ex1-pareto`, `m09-ex2-multivari` (a smaller 3×3×3 design, seed chosen so positional variation dominates instead — the deliberate contrast to the main worked example).
- Two real bugs caught and fixed before committing, neither by an automated check: (1) three rounding mismatches (`sanity.py` caught these: a stored value like 1.20925 rounds to "1.2092" under Python's banker's rounding, not the "1.2093" I got by hand-applying round-half-up — fixed by matching the tool's actual rounding, not intuition, in three spots); (2) an SVG geometry bug in the box-plot figure (the "USL 12.020" text label extended about 14 px past the viewBox's right edge) that `sanity.py` cannot see at all, since attribute values inside SVG tags are stripped before the traceability scan — caught only by a small ad hoc script run in the browser (`el.getBBox()` on every element, compared against the `viewBox`) after a screenshot came back blank (the Browser pane was hidden). **Lesson for future modules with hand-built SVG figures:** run a bounding-box-vs-viewBox check like this one after building any new figure, not just a visual screenshot, since the pane's hidden state makes screenshots an unreliable check on this project and `sanity.py` provides no coverage for SVG geometry at all.
- Browser (port 8781): no console errors; both new SVG figures pass the bounding-box check with no element outside its viewBox; quiz grades 10/10. `verify_all.sh`: 138 examples, 3,791 quantities agree between Check 1 and Check 2, 8,211 values agree between Check 1 and the JavaScript engines, 1,252 page checks. `course.js`: Module 09 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 10

- Built `modules/10-root-cause.html` (Root cause analysis): cause-and-effect diagrams (Ishikawa, a hand-drawn SVG fishbone for leak-test rejects across the six standard categories); 5 Whys with the evidence-at-every-step discipline; Kepner-Tregoe's Is/Is-not; a fault tree introduction (NASA handbook, AND/OR gate formulas); process FMEA per the AIAG-VDA handbook's seven steps, S-O-D ratings and RPN, with Action Priority explained conceptually (Severity first) but its official lookup table deliberately not reproduced, since this course's sourcing for it does not go beyond a secondary summary; prioritization (RPN/AP vs a Pareto chart of history, and why they can disagree). Two worked examples (a 7-failure-mode PFMEA on the brazing process, showing a high-severity/low-RPN failure mode by name and explaining why that is exactly what Action Priority logic exists to catch; a 4-level fault tree on a leak escaping the test undetected, deliberately distinguished from Module 3's 4.2% leak-test *reject* rate — this tree quantifies the much smaller *undetected-escape* probability, about 0.41%, and shows the test's own 5% miss rate contributing about as much residual risk as all three physical leak causes combined, an argument for a Module 4 MSA study on the leak test itself), seven common mistakes, two exercises with collapsible solutions, ten-question quiz, six takeaways, seven references (6 verified/catalogue, 1 secondary, matching `SOURCES.md`'s count). About 3,300 words of prose. No calculator (none of the nine required ones fits; matches the Module 2/3 precedent for qualitative-heavy modules).
- Two new kinds, both pure arithmetic: `rpn` (S×O×D per failure mode, sorted descending; `compute.py` via a pandas DataFrame and `sort_values`, `recompute.py` via plain lists and a hand-written insertion sort — a real implementation difference despite the underlying triviality) and `fault_tree` (a nested dict of AND/OR/basic nodes, recursively evaluated to a top-event probability; both `compute.py` and `recompute.py` have their own independent recursive evaluator). `sanity.py` checks S/O/D ranges, RPN = S×O×D, descending sort order, and (for fault trees) every node probability in [0,1] and the top node's own stored probability matching the tree's computed top_probability.
- **A real bug caught and fixed by the harness itself, not a human review:** `recompute.py`'s first version of `r_rpn()` put the failure-mode name strings into the same flat dict as the numeric S/O/D/RPN fields, and `verify_all.sh` crashed with `TypeError: unsupported operand type(s) for -: 'str' and 'str'` inside `close()`, which unconditionally tries `abs(a - b)`. Fixed by dropping the string fields from the comparison dict entirely, matching `r_pareto`'s existing precedent (compare the sorted numeric outputs; a mismatched string pairing would almost certainly show up as a mismatched RPN at that index anyway, so nothing meaningful is lost). **Lesson for any future kind whose output includes both categorical labels and numbers:** only put genuinely numeric fields into a `recompute.py` comparison dict; `close()` has no string-handling branch and was never meant to have one.
- Datasets: `m10-pfmea-braze` (7 failure modes, seed-free — S/O/D chosen by hand as plausible, varied ratings, not generated data, since there is no natural "seed" for expert judgement ratings), `m10-fault-tree-leak` (3-level nested tree, basic-event probabilities chosen so the "Braze joint leaks" branch lands close to 4.2% for continuity with `m03-charter-leak`), `m10-ex1-pfmea`, `m10-ex2-fault-tree` (exercises).
- Browser (port 8781): no console errors; the fishbone SVG passes the bounding-box-vs-viewBox check (see the Module 9 lesson on this) with nothing out of bounds; quiz grades 10/10. `verify_all.sh`: 142 examples, 3,860 quantities agree between Check 1 and Check 2, 8,211 values agree between Check 1 and the JavaScript engines, 1,314 page checks. `course.js`: Module 10 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 12

- Built `modules/12-correlation-regression.html` (Correlation and regression): correlation and its traps (Anscombe's quartet called back from Module 1, NIST 4.4.4's statement that summary statistics alone cannot certify a fit); simple linear regression (formula, least-squares fit, the standard error of the slope and its t-test tying back to Module 11); residual analysis per NIST 4.4.4 (linearity, constant variance, independence, normality — what each violation looks like in a residual plot); multiple regression introduction (the model, coefficient interpretation "holding other predictors constant", the F-test of the overall model); what R² does and does not tell you (never decreases when a predictor is added; adjusted R² penalizes that; neither implies causation or a good fit by itself). Three worked examples: (1) a four-panel residual-plot SVG built from all four Anscombe series, showing set 2's clear parabola and set 3's single-outlier-driven fit even though all four share the same r and R²; (2) simple regression of braze fillet width on furnace temperature alone (R² 0.477) versus (3) the same fillet width on temperature + flux mass + gap in a multiple regression, where flux mass's simple relationship (visible in a scatter plot) turns out not significant once temperature and gap are already in the model (p=0.1295) — the module's central teaching point, that a predictor's own coefficient and p-value in the full model are what matter, not its simple correlation with y. Seven common mistakes, one exercise (wire-bond pull strength vs bond force alone vs bond force + bond time, the mirror-image case where the second predictor *is* significant, p=0.00283 — deliberately contrasted with Worked example 2 in the solution text) with a collapsible worked solution, ten-question quiz, six takeaways, five references (NIST 4.4.4, Anscombe 1973, the NIST e-Handbook master citation, Montgomery catalog-depth, ASQ BoK). About 3,700 words of prose. No calculator: neither `regression` nor `mregression` is among the nine required calculator types (matches the `test_calculators.js` `SKIP_KIND` comment already on file for Module 12, and the Module 2/3/9/10 precedent for content that doesn't fit one of the nine); `Stats.regression` still exists as a tested engine (Check 4 covers `m12-temp-fillet` and `m12-ex1-force-only`, both kind `regression`, through it) even with no UI calculator on the page.
- One significant new kind, `mregression` (multiple linear regression, 3 or 2 predictors): `compute.py` via `statsmodels.formula.api.ols`; `recompute.py` solves the normal equations XᵀXβ=Xᵀy by hand, implementing Gauss-Jordan elimination with partial pivoting from scratch (a `matrix_inverse` helper) to invert XᵀX for the coefficient covariance matrix, then reuses the existing `t_cdf`/`f_sf` for the per-coefficient and overall-F p-values — a genuinely independent route from `statsmodels`, not a restatement of it. Matched on the first attempt: every coefficient, SE, t-stat, p-value and R²/adjusted R²/F agreed with `statsmodels` to the rounding shown on the page. `sanity.py` gained `check_mregression` (R²adj ≤ R², F and its p-value consistent with the coefficient count and residual df, R² in [0,1]).
- Two real bugs caught before committing, both in `recompute.py`, neither in the final harness: (1) an early `r_attribute_agreement`-style nested-dict return pattern was tried for `r_mregression` too and rejected before it caused the same `close()` crash Module 4 hit — flattened to dotted-path keys (`coefficients.temp.coef`, `coefficients.temp.p`, etc.) from the start, matching the established convention; (2) the exercise dataset `m12-ex1-force-only` (bond force alone, no bond time) was registered and verified as a separate simple-regression example rather than re-deriving a one-predictor result from the two-predictor `m12-ex1-mreg` fit, since `mregression`'s harness has no single-predictor code path and forcing one in would have been a bigger change than a second small dataset.
- Datasets: `m12-temp-fillet` (24 joints, fillet width vs temperature alone), `m12-mreg-fillet` (same 24 joints, three predictors — the flux-mass-not-significant teaching point), `m12-ex1-force-only` and `m12-ex1-mreg` (20 wire-bonds, exercise 1's one- and two-predictor fits — bond time *is* significant here, the contrasting case). Anscombe reused `m01-anscombe-1..4` with no new registration, just redeclared in `<meta name="examples">`.
- Browser (port 8781): no console errors, no NaN; the four-panel residual-plot SVG passes the bounding-box-vs-viewBox check with nothing out of bounds; nav pager links to Module 11 and Module 13 correctly; quiz grades 10/10. `verify_all.sh`: 146 examples, 4,042 quantities agree between Check 1 and Check 2, 8,339 values agree between Check 1 and the JavaScript engines (114 examples with a calculator engine), 1,357 page checks. `course.js`: Module 12 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 14

- Built `modules/14-lean-tools.html` (Lean improvement tools; note the file slug is `14-lean-tools`, not `14-lean-improvement-tools` — checked `course.js`'s existing `MODULES` entry before naming the file, since it was registered with this slug ahead of time): 5S (Hirano) and standard work (Lean Enterprise Institute's three elements: takt time, work sequence, standard in-process stock, newly verified this session as source S-E38) as the stable foundation the rest of the toolkit depends on, tied explicitly to being a precondition for a control chart's common-cause variation meaning what it should (Modules 16/17 callback); poka-yoke (Shingo) and its three mechanisms, tied to Module 10's PFMEA Occurrence/Detection distinction; SMED (Shingo) with the internal/external setup method, Shingo's own numeric claims explicitly framed as "Shingo reports," not independently verified, per `SOURCES.md`'s existing guidance; kaizen events (Imai); solution selection with a Pugh matrix (Pugh, secondary source), including the explicit caution that it is a facilitation tool, not a decision algorithm, and that a weighted variant exists; piloting and proving an improvement with data, the module's central methodological point — a pilot needs both a hypothesis test with an effect size AND a control chart of each period on its own, not either alone. Real-world example: 3M 2007 (BusinessWeek, S-E2, already verified in Module 0) as the cautionary story about applying efficiency/standardization tools to genuinely exploratory work, reused here with a Lean-specific framing rather than re-verified. Three worked examples: (1) the SMED changeover t-test, with an explicit unequal-variances check (Levene's p=0.0084) leading to Welch's test over the pooled test, though the conclusion does not change here because the effect (Cohen's d=4.70) is enormous; (2) the Pugh matrix, where the net winner (modular quick-change, net +3) is not the cheapest concept, and a concept that wins the single criterion the team cares most about (ceramic insert, gap repeatability) still nets to −2 once every criterion counts; (3) a two-period leak-rate pilot (15 lots before, 14 after) that deliberately cannot be proven by its own p-charts' 3-sigma rule alone, since the lower control limit floors at 0.000 in both periods at this rate and these lot sizes — proven instead with a 2×2 chi-square test (χ²=10.524, p=0.0012) plus relative risk (0.455) and risk difference (−2.41 points) as the effect-size companions, with an explicit warning that Cramér's V (0.064) looks small here only because leak events are rare in both periods, not because the effect is unimportant. Two exercises (a second, smaller SMED-style pilot with a more marginal, non-obvious effect size; a second Pugh matrix with a deliberate twist — the concept tied for the best single-criterion score is not the net winner), ten-question quiz, six takeaways, seven references. About 5,175 words of page text including tables/quiz/references (prose alone is smaller; not separately counted). Three calculators embedded, all reusing existing calculator types rather than introducing a new one: the "tests" calculator (SMED data) and two "control" calculators (pilot before/after p-charts) — checked against precedent first (`grep` across `modules/*.html` for `data-calc="tests"` and `data-calc="control"`) and confirmed that other modules already reuse these two calculator types outside their "home" module (Module 5, 8, 19 all embed a "control" calculator; Module 19 also embeds "tests"), so Module 14 does the same rather than omitting a calculator by default.
- One new kind, `pugh_matrix` (concept-selection scoring relative to a baseline, pure summation): `compute.py` via a small pandas DataFrame; `recompute.py` via plain dict loops, matching Module 10's `rpn` precedent for keeping the numeric fields (`pluses`/`minuses`/`sames`/`net`, per concept) in the `recompute.py` comparison dict but dropping the string fields (`winner`, `ranking`) before they reach `close()`, which has no string-handling branch (documented in-line in `recompute.py` this time, not just in `PROGRESS.md`, so the next kind that returns a mix of labels and numbers does not have to rediscover this the hard way). `sanity.py`'s `check_pugh_matrix` verifies net = pluses − minuses, the three counts sum to the criterion count, the baseline's own net is 0, `winner` is the head of `ranking`, `ranking` is a permutation of `concepts` sorted by net descending, and `winner` actually has the maximum net score — all self-consistency checks on `compute.py`'s own output, since `winner`/`ranking` (strings) cannot be cross-route-verified against `recompute.py` the way the numeric fields are.
- A second small addition, not originally planned: `relative_risk` and `risk_difference_pct` fields added to the existing, shared `chisq` kind (guarded by `if r == 2 and c == 2`, since relative risk is only defined for a 2×2 table), because CLAUDE.md requires an effect size alongside every hypothesis test and the chi-square test alone gives significance but not a practically meaningful number. Added to `compute.py`, `recompute.py` (both independently, matching the pattern everywhere else), and `sanity.py` (checks the two new fields against the already-checked `row_proportions`). Because `chisq` is shared, this also silently improved the three pre-existing 2×2 examples from other modules (`m11-chisq-shift`, `m11-ex2-chisq-leak`, `m19-before-after`) — re-ran the full suite after the change specifically to confirm none of the three broke; all three passed with the new fields present. `chisq` remains outside `test_calculators.js`'s scope (no calculator fits a contingency table; unchanged from Module 11's original note), so this addition needed no JavaScript changes.
- Datasets: `m14-smed-changeover` (16+16, before/after SMED), `m14-pugh-fixture` (5 concepts × 5 criteria, hand-set expert-judgement scores, no seed, matching `m10-pfmea-braze`'s precedent for the same reason), `m14-pilot-before`/`m14-pilot-after` (15+14 lots, p charts) and `m14-pilot-chisq` (their totals as a 2×2 table), `m14-ex1-changeover` (10+10, a smaller/more marginal effect than the main SMED example, seed-searched for p in [0.0005, 0.01] specifically so the exercise needs an actual t-table lookup rather than being obviously significant by eye), `m14-ex2-pugh` (3 concepts × 4 criteria, deliberately designed so the concept tied for the best single-criterion score is not the net winner).
- No bugs this module; the `pugh_matrix` recompute.py string/close() issue was anticipated and avoided from the start (documented as a known trap since Module 10), and the seed searches for both SMED examples and both pilot p-chart periods succeeded within the first search script on the first run.
- Browser (port 8781): no console errors, no NaN, no `undefined`; all three calculators render correctly and their computed values (p̄ 0.0441/0.0201, means 68.544/26.769, t=13.306, p<0.001) match the page's `data-ex` numbers to display precision; nav pager links to Module 13 (previous) correctly and shows Module 15 as "Not yet published" (correct, since it is next in the list but not yet built); quiz grades 10/10. `verify_all.sh`: 153 examples, 4,159 quantities agree between Check 1 and Check 2, 8,488 values agree between Check 1 and the JavaScript engines (118 examples with a calculator engine), 1,415 page checks. `course.js`: Module 14 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 15

- Built `modules/15-variation-and-design.html` (Variation reduction and design): tolerance stack-up revisited (worst case = Σtol vs RSS = √Σtol², the independence and centring assumptions behind RSS, Evans' survey); allocating a stack's tolerance budget using RSS's quadratic relationship (loosen the expensive-to-hold part, tighten the cheap one); robust design's core idea (parameter design before tolerance design) with the SN-ratio statistical critique (Box 1988, S-C29, newly cited in this module though verified earlier for a different module's outline) presented as the reason this course prefers analyzing mean and log-variance separately rather than Taguchi's original ratios; the Taguchi loss function (k = A₀/δ₀², average loss = k·(s²+(mean−target)²)) with its variance/off-centre decomposition; feeding capability data back into DFM as a closing synthesis. Real-world example: the Sony USA-vs-Japan colour-density story, explicitly labelled "as reported by Taguchi and Clausing and by Phadke" per `SOURCES.md`'s standing decision (S-E24/S-E25) — deliberately no specific numeric figures invented or restated for the real story itself, since this session did not re-read the underlying source; the same argument is instead worked on fully traceable constructed numbers in Worked example 2. Three worked examples: (1) a three-part gap stack where worst case (0.115mm) fails a 0.100mm spec half-width but RSS (0.0687mm) passes, predicted Cpk 1.455 (centred, Cpk=1.0/part assumed) versus a 200,000-run Monte Carlo with a realistic 1σ per-part centring bias giving simulated Cpk 0.901 (~3,530 ppm out of spec) — RSS's spread prediction held almost exactly (simulated sd 0.02283mm vs predicted 0.02291mm), all the lost capability came from the centring assumption alone; (2) Taguchi loss on the Module 7 bore data reused directly (no new bore dataset), where the single worst reading's loss came out to exactly A₀ because that reading sat exactly at the spec limit, the boundary the loss function is calibrated against by construction — a clean, unplanned coincidence from reusing real capability data rather than fabricating a dataset for the point; (3) reallocating Worked example 1's own stack (loosen the machined feature, tighten the purchased shim) at a more typical Cpk=1.33 assumption, showing RSS rises only modestly (0.0687→0.0737mm) despite a 20% loosening on the largest tolerance. Seven common mistakes, two exercises (a smaller two-part clearance stack; a Taguchi loss calculation on a fresh 25-package fill-weight dataset, deliberately showing a larger off-centring share, 33.81% vs Worked example 2's 25.15%), ten-question quiz, six takeaways, eight references. About 4,077 words of page text (`#main` innerText). No calculator (neither new kind fits any of the nine required types, matching the Module 12/2/3 precedent for qualitative- or simulation-heavy content).
- Two new kinds. `tolerance_stack`: `compute.py` builds the closed-form worst-case/RSS/predicted-Cpk fields with plain arithmetic (numpy only for convenience, not for anything statistically load-bearing) and a 200,000-draw Monte Carlo via `numpy.random.default_rng`, applying a fixed per-part mean shift (not re-randomised per assembly, modelling a process that is quietly off-target rather than one with more piece-to-piece noise) in the direction that pushes the stack the same way for every part. `recompute.py`'s closed-form fields are exact plain-Python arithmetic (compared exactly); its Monte Carlo route reuses the `_XorShift` generator already built for Module 16's `arl` kind (own seed, `20261015`, unrelated to `compute.py`'s numpy seed) rather than importing Python's own `random` module, matching this file's existing practice of not depending on any PRNG beyond its own from-scratch one. Only `simulated.mean` and `simulated.cpk` are cross-route-compared, via a new `path.startswith("simulated.")` case in `close()` (tolerance 3%, sized from ten independently-seeded 200,000-draw replications of the exact main-example scenario, which agreed to a run-to-run sd of about 0.2% on the simulated Cpk — so 3% is roughly 15× that, generous without being meaningless); `simulated.sd` and `simulated.ppm` are deliberately left out of that comparison (their own Monte Carlo standard error, especially `ppm`'s as a rare-event count, is too large a fraction of their small scale for one shared tolerance to be honest for them and for `mean`/`cpk` at once) and are instead range/self-consistency-checked by `sanity.py` against `compute.py`'s own single output (`simulated.sd` within 15% of the closed-form `sigma_stack_predicted`, `simulated.ppm` in [0, 1e6]). `sanity.py` also checks RSS ≤ worst case always, the two boolean spec flags are self-consistent, and — the most load-bearing relationship check in this module — that a positive `shift_sigma` always produces `simulated.cpk` below `cpk_predicted`, which is the whole pedagogical point of Worked example 1 and would have caught a sign error in either route's shift-direction logic. `taguchi_loss`: `compute.py` and `recompute.py` are both closed-form (no Monte Carlo needed), using the *biased* (n-divisor, not n−1) mean square specifically because that is the convention under which mean((x−target)²) = variance + (mean−target)² holds as an exact identity; `recompute.py`'s route is plain Python sums, no numpy. `sanity.py` checks k = A₀/δ₀², avg_loss = var_component + offcenter_component exactly, total_loss = n × avg_loss, and max_loss ≥ avg_loss.
- Datasets: `m15-stack-three-parts` (3-part gap stack, the main worked example), `m15-alloc-three-parts` (same stack, tolerance reallocated), `m15-loss-bore` (reuses `m07-bore`'s 125 raw values directly rather than inventing a new bore dataset — CLAUDE.md and this session's own precedent both favour reuse over a needless new registration when an existing dataset fits exactly), `m15-ex1-stack` (2-part clearance stack, gentler 0.5σ bias), `m15-ex2-loss` (25-package fill-weight, fresh seeded data, seed 0).
- **One real bug, caught by `sanity.py`, not by review:** Exercise 2's 25-value fill-weight table was first drafted with plausible-looking numbers typed from memory of the seed-search script's summary statistics (mean/sd only — the script never printed the individual 25 values), rather than extracted from the actual generated CSV. This is precisely the "hand-typed data" trap already documented from Module 5's session, and it reproduced exactly as predicted: `sanity.py`'s free-number scan flagged 15 of the 25 fabricated values as untraceable to `results/` or the CSV (the other 10 happened to round to numbers that were coincidentally already `allowed` for unrelated reasons). Fixed by reading `verification/data/m15-ex2-loss.csv` directly with the Read tool and replacing the table with the exact values. **Lesson reaffirmed for any future module:** never type a data table from a script's printed summary statistics; always read the actual generated CSV (or extract it programmatically) for anything embedded verbatim on a page.
- `recompute.py`'s Monte Carlo route added real runtime: recompute.py alone went from a few seconds to about 36 seconds (two tolerance_stack examples × 200,000 draws × 2-3 parts each, in pure Python with a Box-Muller transform per draw), and the full `verify_all.sh` correspondingly took longer than the roughly 3 minutes recent modules have taken. Still well within a single background-task wait; noted here in case the trend continues once Module 18 is added.
- Browser (port 8781): no console errors, no NaN, no `undefined`; quiz grades 10/10; nav pager links to Module 14 (previous, correct) and Module 16 (next, already built by the parallel session, correct). `verify_all.sh`: 158 examples, 4,196 quantities agree between Check 1 and Check 2, 8,488 values agree between Check 1 and the JavaScript engines (118 examples with a calculator engine), 1,451 page checks. `course.js`: Module 15 `built: true`.

### 2026-09-10, session 6 (continued): Phase D, Module 18 — the last module in the author's requested batch

- Built `modules/18-control-plans.html` (Control plans and sustaining gains): control plans (AIAG's standalone *Control Plan* manual, 1st ed. March 2024, its APQP linkage and "Safe Launch" phase); standardization and documentation as what a control plan assumes is already in place (explicit callback to Module 14's standard work: a control chart's limits describe a process running one documented way); handoff to production (naming an owner, training the actual long-term shift, not just the pilot shift); linking to PPAP (control plan as a required submission element) and IATF 16949 clause 9.1.1.1 (process studies, capability verification, a reaction plan when unstable or not capable, recording significant events); reaction plans, with an explicit callback to Module 2's funnel experiment — an operator "correcting" every reading that drifts toward a spec limit is running the funnel experiment's Rule 2 on a real process, by hand, which is the single most important idea this section carries; ongoing capability monitoring as the **third and final** of this course's three required treatments of "control limits are not specification limits" (confirmed by grepping Modules 7 and 16 first, rather than assuming — Module 16's own takeaway already states "for the second of three times in this course," which settled where Module 18 needed to land as the third), here extended from within-one-chart to across-months via Ppk confidence-interval overlap; project closure and reporting. No real-world case with data exists for this module per `SOURCES.md`'s own note (standards only), so "Why this matters" uses a clearly labelled illustrative failure (a DMAIC gain eroding unnoticed after a handoff with no control plan) rather than a fabricated real case. Three worked examples, deliberately built to need **no new computed statistics**, only citation of already-verified numbers from elsewhere in the course (Module 18's own `<meta name="examples">` redeclares `m07-bore`, `m04-grr-bore`, and `m03-charter-leak` for exactly this reason, the same redeclaration pattern Module 12 used for Anscombe): (1) a full control plan row for the bore diameter, citing Module 7's Ppk (1.040) and Module 4's %GRR (29.8%, AIAG "conditional" band) and ndc (5) directly; (2) the matching reaction plan table (investigate the assignable cause, never "adjust toward nominal"), with the funnel-experiment argument worked through in prose; (3) five months of the bore feature's ongoing Ppk with its 95% CI, months 1-4 (Ppk 1.029 to 1.237) all overlapping heavily (nothing distinguishable from noise) versus month 5 (Ppk 0.517, CI 0.338-0.695) sharing no overlap at all with any of the first four's lower bounds (lowest is 0.739) — a real, dual mean-and-spread shift. Seven common mistakes, two exercises (a control-plan-row exercise for an attribute characteristic, the leak test, citing Module 3's charter baseline and asking the learner to reason about which control method fits pass/fail data; a three-month "real shift or noise" exercise on a fresh wall-thickness dataset, structurally identical to Worked example 3 but shorter, so the learner applies the CI-overlap logic themselves rather than only reading it), ten-question quiz, six takeaways, seven references (5 verified, 2 secondary, matching `SOURCES.md`'s count exactly). About 3,733 words of page text (`#main` innerText). No calculator (control plans and reaction plans are qualitative; the ongoing-Cpk-trend content reuses the existing `capability` kind and its results, already covered by the capability calculator elsewhere, and does not introduce anything a calculator instance on this page would add).
- **No new harness kind, as planned**: "ongoing Cpk trend" is eight new registrations of the existing, long-since-verified `capability` kind (five "months" for the main worked example, three "months" for exercise 2), reading off the `overall.Ppk_ci95` field that kind has computed since Module 7. This is the lightest module of the six built this session in terms of new `compute.py`/`recompute.py`/`sanity.py` code: none was needed.
- Datasets: `m18-month1` through `m18-month4` (the same bore feature as `m07-bore`, 6 subgroups of 5 each, mu fixed per month via a pre-computed nested-seed draw rather than replicated in `generate.py` — computed once via a scratch script, then hardcoded as a literal float per month, avoiding the need to reproduce a nested `np.random.default_rng` call chain exactly inside `generate.py` itself), `m18-month5` (a deliberate shift, both mean and sigma moved), `m18-ex2-montha`/`-monthb`/`-monthc` (a different characteristic, injection-moulded wall thickness, individuals/I-MR rather than subgrouped, for variety and to keep the exercise visibly distinct from the worked example rather than a copy with different numbers). Every seed search in this module succeeded within one or two script iterations by first computing the required per-part sigma from a target Ppk band directly (spec half-width / (3 × target Ppk)) rather than guessing sigma and hoping, a faster approach than the trial-and-error range-search pattern used for some earlier modules' seed hunts.
- **One near-miss caught before it became a bug**: month C of exercise 2 was initially registered using the *sample* mean/sd my search script had printed (2.5422, 0.07983) as the generator's *population* mu/sigma, instead of the population parameters actually fed to the generator (2.53, 0.062) — these are different numbers precisely because of sampling noise at n=25, and passing the wrong pair in would have silently produced a completely different, unvalidated dataset rather than reproducing the checked scenario. Caught by re-reading my own search script's call arguments side by side with the draft registration before running `compute.py`, not by any automated check (a wrong-but-still-internally-consistent dataset would have passed every one of the four checks, since it would just describe a different, equally-valid random draw — this class of bug is invisible to the harness by construction and only catchable by checking the registration's actual parameters against the script that validated them). **Lesson for future seed searches:** when a search script prints both the generation parameters and the resulting sample statistics, copy the generation parameters into the registration, never the sample statistics that happened to result from them.
- Browser (port 8781): no console errors, no NaN, no `undefined`, no leftover drafting artifacts (a stray "wait, mm" aside was caught and removed from Exercise 2's solution text before this check, not after); quiz grades 10/10; nav pager links to Module 17 (previous, correct) and Module 19 (next, correct) — the full course, Modules 0 through 19, is now linked end to end for the first time this session. `verify_all.sh`: 166 examples, 4,678 quantities agree between Check 1 and Check 2, 9,351 values agree between Check 1 and the JavaScript engines (126 examples with a calculator engine, up from 118 — the capability calculator now also covers all 8 new monthly datasets), 1,498 page checks. `course.js`: Module 18 `built: true`.
- **This is the last module in the author's explicit request** ("work on phase D modules 3, 4, 5, 6, 9, 10, 12, 14, 15, 18"). All ten are now built, verified, and committed. Phase E (the full verification pass across every module) and Phase F (ship) are not part of this request and were not started; see the "Resume here" line below for what a future session should do next, which is ask the author rather than assume.
